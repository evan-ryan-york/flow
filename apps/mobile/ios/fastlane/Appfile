# App Store Connect configuration for Flow
# Credentials can be set via environment variables or entered at runtime

app_identifier("com.flow.app")  # Bundle ID
team_id("WDDXQKN8VJ")                  # Developer Portal Team ID

# Apple ID: Set FASTLANE_USER env var, or Fastlane will prompt at runtime
# Password: Set FASTLANE_PASSWORD env var, or Fastlane will prompt and save to Keychain

# For App Store Connect API (recommended for CI/CD):
# 1. Generate API key at https://appstoreconnect.apple.com/access/api
# 2. Download the .p8 file
# 3. Set these env vars:
#    - APP_STORE_CONNECT_API_KEY_KEY_ID
#    - APP_STORE_CONNECT_API_KEY_ISSUER_ID
#    - APP_STORE_CONNECT_API_KEY_KEY (base64 encoded .p8 contents)
