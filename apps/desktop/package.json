{
  "name": "desktop",
  "version": "1.0.22",
  "description": "Flow Desktop",
  "main": "index.js",
  "scripts": {
    "dev": "tauri dev",
    "build": "mkdir -p ../web/out && tauri build",
    "preview": "tauri build --debug"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.6.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0"
  }
}
